services:
  caddy:
    configs:
      - source: Caddyfile
        target: /etc/caddy/sites_enabled/Caddyfile

configs:
  Caddyfile:
    content: |
      ${COMPOSE_PROJECT_NAME}.ipn.rahulsalvi.com {
        reverse_proxy proxy_target:8080
      }
